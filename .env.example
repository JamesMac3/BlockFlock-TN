VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public Turnstile site key — safe to expose in frontend code/bundles.
# The matching TURNSTILE_SECRET is server-only and must never be set here
# or anywhere under VITE_*; it belongs in the Supabase Edge Function's
# secrets (newsletter-subscribe) and in the Supabase Auth CAPTCHA settings.
VITE_TURNSTILE_SITE_KEY=
