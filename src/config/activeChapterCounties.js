// Temporary public chapter configuration. Replace this with a dedicated,
// administrator-controlled county field—not portal-account inference.
export const ACTIVE_CHAPTER_COUNTY_SLUGS = new Set([
  "rutherford-county",
]);
